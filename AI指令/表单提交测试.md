# Project: RuoYi 管理系统测试
# Context
- Base URL: http://192.168.142.146/login?
- Credentials: 用户名 admin, 密码 admin123
== 生成测试计划 ==
mode:plan designer
# Task Description
请为“用户管理”模块设计深度测试计划。你需要模拟人类操作完成以下闭环：
1. [登录与导航]: 
   - 登录系统。
   - 展开侧边栏“系统管理”，点击“用户管理”。
   - 验证 URL 是否跳转至 `/system/user`。
2. [新增用户功能]:
   - 点击“新增”按钮。
   - 检测表单需要添加的数据
   - 填入合法数据（自行生成）
   - 预期结果：提交后拦截到 `POST **/system/user` 接口返回 200，且页面弹出“操作成功”。
3. [修改用户功能]:
   - 在用户列表中通过“用户名”搜索刚才创建的 `test_ai`。
   - 点击“修改”，将昵称改为“AI测试-已修改”。
   - 预期结果：拦截 `PUT **/system/user` 接口，并验证列表中昵称已更新。
4. [删除用户功能]:
   - 选中 `test_ai` 对应的删除按钮。
   - 在二次确认弹窗中点击“确定”。
   - 预期结果：验证后端接口返回成功，且列表中不再存在该用户。
# Output Format
请以 JSON 数组形式输出所有步骤，确保 step_id 连续。
生成完以后请删除所有多余的表单，以免下一个模型产生误解

== 测试编写脚本 ==
切换至script engineer模式
从工作区找到计划表
理解计划流程
检测新增后的表单需要填写的内容
生成测试脚本

== 开始测试 ==
切换至debugger
运行测试脚本
执行编写好的脚本检查是否存在错误并且进行撰写报告
仅仅保留可复现的测试的脚本，删除多余脚本