# 模式规定
- 每一次前端测试都要开启有头模式


# 文件命名规范
## 保存路径规范
每次开启新任务都需要创建一个新的文件
文件夹命名格式规范
```
[时间]+[任务名称]
```
每次任务生成的文件都必须要新建一个独立文件夹里保存：
```
/工作区/[新文件夹]
```
## 生成文件规范
所有生成的文件都必须按照一下规范进行生成：
```
[日期]_[模块/功能]_[测试内容]_[版本].[文件后缀]
```

禁止生成任何没有被允许的文件夹，禁止自行命名创造新的文件夹
