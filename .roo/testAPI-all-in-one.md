---
description: "【全链路流水线】按序执行：配置 -> 提取 -> 基准(闭环) -> 鉴权 -> Fuzz -> 边界 -> 报告"
---

- **阶段一：环境初始化 (/configure)**
    - 通过读取读取.roo/commands目录下对应的斜杠命令.md文件并作为该阶段的任务流执行
- **阶段二：API 提取与逆向准备 (/extractAPI)**
    - 通过读取读取.roo/commands目录下对应的斜杠命令.md文件并作为该阶段的任务流执行
- **阶段三：基准与闭环测试 (/test-APIbase)**
    - 通过读取读取.roo/commands目录下对应的斜杠命令.md文件并作为该阶段的任务流执行
- **阶段四：鉴权绕过测试 (/test-NOauth)**
    - 通过读取读取.roo/commands目录下对应的斜杠命令.md文件并作为该阶段的任务流执行
- **阶段五：健壮性 Fuzz 测试 (/test-fuzz)**
    - 通过读取读取.roo/commands目录下对应的斜杠命令.md文件并作为该阶段的任务流执行
- **阶段六：边界值测试 (/test-Bound)**
     - 通过读取读取.roo/commands目录下对应的斜杠命令.md文件并作为该阶段的任务流执行
- **阶段七：报告生成 (/report)**
     - 通过读取读取.roo/commands目录下对应的斜杠命令.md文件并作为该阶段的任务流执行
- **全局控制流**
    - 脚本执行顺序：每个阶段直接读取.roo/commands目录下对应的斜杠命令.md文件，相当于直接执行这些斜杠命令
    - **串行锁**：严格遵守“上一步骤完全结束（脚本执行完毕并生成 JSON）”后，才能开始下一步
    - **状态保持**：全流程共享 Token，若中途 401 则自动触发重连
    - **模式切换**：每个脚本生成前必须确保处于 `script engineer` 模式，执行时切换至 `debugger` 模式