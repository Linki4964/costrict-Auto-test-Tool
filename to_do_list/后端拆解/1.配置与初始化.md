# 🔧 前置任务：配置与初始化（`configure.py` 核心逻辑）
## 功能目标
- 收集必要上下文信息
- 触发后续脚本生成流程

## 关键输入（必须在这一步向用户请求参数）！！！
先向用户提供填写模板
1. **源码根路径**（用于静态分析）（默认D:\desktop\AI Coding\RuoYi-Vue）
2. **目标系统 Base URL**（默认192.168.236.128）
3. **工作目录**（用于存储中间结果，默认当前目录）
4. **用户账号密码**（默认admin/admin123）

在获取完这些信息后再进行configure.py脚本的编写！！！
## 输出
1. 生成 project_config.json 配置文件
2. 包含：源码路径、目标URL、工作目录等


## 切换至script engineer模式

## 核心操作
### Authentication token获取逻辑
```python
def fetch_and_save_token(base_url: str, username: str, password: str):
    import requests, json
    resp = requests.post(f"{base_url}/prod-api/login", json={"username": username, "password": password})
    if resp.status_code == 200 and resp.json().get("code") == 200:
        token = resp.json()["token"]
        config = {"auth_token": token}
        with open("project_config.json", "w") as f:
            json.dump(config, f, indent=4)
```
### 项目结构分析逻辑
```python

def capture_project_structure(source_path: str):
    # 在当前工作目录执行系统命令：
    #   dir "{source_path}" /s 
    # 目的：生成模型可读的目录与文件清单，用于技术栈识别
    pass
```
> ⚠️ 注意：此步骤需在写完configure.py脚本后立即运行，因为此步骤需要脚本的结果需要给予模型识别并解析，为任务一及后续的任务提供依据！！！

> ⚠️ 注意：此步骤需由模型自行确认技术栈，并返回对应解析器 

---